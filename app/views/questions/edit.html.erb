<%= form_for(@question) do |f| %>
   <!-- put in error messages here -->
   <%= f.text_field :title, class: "form-control"%>
   <%= f.text_area :content, class: "form-control" %>
   <%= f.submit "Save Changes", class: "btn btn-primary" %>
<% end %>
