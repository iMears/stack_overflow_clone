<%= form_for(@question) do |f| %>
   <!-- put in error messages here -->
   <%= f.label :title %>
   <%= f.text_field :title %>
   <%= f.label :content %>
   <%= f.text_field :content %>
   <%= f.submit "Save Changes" %>

<% end %>
