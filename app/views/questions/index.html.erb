<% @questions.each do |question| %>
  <h1> <%= question %> </h1>
  <h1> <%= question.title %> </h1>
  <h2> <%= question.content %> </h2>
<% end %>

<%= form_for :question do |f| %>
  <%= f.text_field :title %>
  <%= f.text_area :content %>
  <%= f.submit %>
<% end %>
